[build]
command = "yarn build"
publish = "server/functions-build/"

[[redirects]]
from = "/api/v1/*"
to = "/.netlify/functions/:splat"
status = 200

[functions]
node_bundler = "esbuild"
directory = "server/functions/"
